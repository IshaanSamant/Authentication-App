<h1>Welcome <%= @user.username %></h1>

  <%= link_to "Edit Credentials", edit_user_path(@user) %><br><br>
  <%= link_to "Logout", root_path %><br><br>
  <%= button_to "Delete Account", user_path(@user),
                  method: :delete,
                  data: { confirm: "Are you sure?" } ,class: "btn btn-outline-danger"%>
